		TypedQuery<MeterReading> query = em.createQuery("SELECT max(total_consumption_adjusted), reading_date_time FROM SmartMeter sm JOIN sm.meterReadings mrs WHERE sm.oid = :smartID AND mrs.readingDateTime BETWEEN :startDate and :endDate GROUP BY DATE(readingDateTime)"
